# Table Module

Future module boundary for the shared campaign table: scenes, layers, canvas,
media placement, table realtime, and master/player room orchestration.

Current runtime code still lives in `components/table/*` and `lib/table/*`.
