# Home Module

Entry module for the app landing page: account access, character shortcuts,
room/role selection, and links into the character sheet, table, journal, and
reference.

`HomeRoute` renders the main screen used by `/` and keeps the App Router page
as a thin module wrapper.
