# Hub Core

The Hub is the thin application orchestration layer: routes, module registration,
chronicle/session concepts, and game-system selection.

It must not contain VTM rules, React table logic, or Supabase feature queries.
